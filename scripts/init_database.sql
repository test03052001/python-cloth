-- Quick init: creates database only.
-- For full schema + sample data, use: scripts/cloth_store.sql

CREATE DATABASE IF NOT EXISTS cloth_store
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE cloth_store;
